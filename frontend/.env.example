# ─── UCAB Frontend — Environment Variables Template ──────────────────────────
# Copy this file to `.env` and fill in your values. NEVER commit the real .env.

# Backend API base URL (used by axios in AuthContext.jsx)
VITE_API_URL=http://localhost:8000/api

# Backend base URL (used for image src construction in Uhome.jsx)
VITE_API_BASE_URL=http://localhost:8000
